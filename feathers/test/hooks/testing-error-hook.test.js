const feathers = require('@feathersjs/feathers');
const testingErrorHook = require('../../src/hooks/testing-error-hook');

describe('\'testingErrorHook\' hook', () => {
  let app;

  beforeEach(() => {
    app = feathers();

    app.use('/dummy', {
      async get(id) {
        return { id };
      }
    });

    app.service('dummy').hooks({
      error: testingErrorHook()
    });
  });

  it('runs the hook', async () => {
    expect.assertions(1);
    const result = await app.service('dummy').get('test');
    expect(result).toEqual({ id: 'test' });
  });
});
